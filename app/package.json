{
  "name": "app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run copy:abi && next dev",
    "copy:abi": "node abi-setup.js",
    "build": "npm run copy:abi && next build",
    "start": "next start"
  },
  "dependencies": {
    "bootstrap": "^5.0.0-beta2",
    "ethers": "^5.0.32",
    "hex2ascii": "^0.0.3",
    "next": "10.0.9",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  }
}
